import type {Assessment,Commitment,Review,ScorecardEntry} from "../types";import {supabase} from "./supabase";async function user(){if(!supabase)throw new Error("Supabase is not configured.");const{data,error}=await supabase.auth.getUser();if(error||!data.user)throw new Error("Please sign in before using the workspace.");return data.user}export async function saveAssessment(a:Assessment,organizationId:string){const u=await user();const{error}=await supabase!.from("assessments").upsert({id:a.id,user_id:u.id,organization_id:organizationId,assessment_type:a.type,period_start:a.periodStart,period_end:a.periodEnd,scores:a.scores,submitted_at:a.submittedAt??null});if(error)throw error}export async function saveCommitment(c:Commitment,organizationId:string){const u=await user();const{error}=await supabase!.from("commitments").upsert({id:c.id,user_id:u.id,organization_id:organizationId,dimension_id:c.dimensionId,title:c.title,action:c.action,timeframe:c.timeframe,evidence:c.evidence,status:c.status});if(error)throw error}export async function saveReview(r:Review,organizationId:string,subjectUserId:string){const u=await user();const{error}=await supabase!.from("management_reviews").upsert({id:r.id,reviewer_id:u.id,subject_user_id:subjectUserId,organization_id:organizationId,assessment_id:r.assessmentId||null,notes:r.notes,barriers:r.barriers,support:r.support,reviewed_at:r.reviewedAt});if(error)throw error}export async function saveScorecard(e:ScorecardEntry,organizationId:string,cycleId:string){const u=await user();const{error}=await supabase!.from("scorecard_entries").upsert({id:e.id||crypto.randomUUID(),organization_id:organizationId,cycle_id:cycleId,user_id:u.id,dimension_id:e.dimensionId,metrics:e.metrics,evidence:e.evidence});if(error)throw error}export async function ensureCurrentCycle(organizationId:string){const today=new Date();const day=today.getDay();const diff=(day+6)%7;const start=new Date(today);start.setDate(today.getDate()-diff);const weekStart=start.toISOString().slice(0,10);const end=new Date(start);end.setDate(start.getDate()+6);const {data,error}=await supabase!.from("weekly_cycles").upsert({organization_id:organizationId,week_start:weekStart,week_end:end.toISOString().slice(0,10),status:"open"},{onConflict:"organization_id,week_start"}).select("id,week_start,week_end,status").single();if(error)throw error;return data}export async function getMyOrganization(){const u=await user();const{data,error}=await supabase!.from("organization_members").select("organization_id,role").eq("user_id",u.id).eq("active",true).limit(1).maybeSingle();if(error)throw error;return data}