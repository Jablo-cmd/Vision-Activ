export type Dimension={id:string;name:string;description:string;weight:number}; export type AssessmentScore={dimensionId:string;score:number;evidence?:string}; export type Commitment={id:string;title:string;action:string;timeframe:string;evidence:string;status:"not_started"|"in_progress"|"complete"}; export type Assessment={id:string;userId:string;type:"baseline"|"weekly";periodStart:string;periodEnd:string;scores:AssessmentScore[];submittedAt?:string}; export type Review={id:string;assessmentId:string;notes:string;barriers:string;support:string;reviewedAt:string}; export const FRAMEWORK_DIMENSIONS:Dimension[]=Array.from({length:12},(_,i)=>({id:"dimension-"+(i+1),name:"Framework Dimension "+(i+1),description:"Configured framework dimension. Replace with the approved Vision Activ framework definition before production rollout.",weight:1/12}));