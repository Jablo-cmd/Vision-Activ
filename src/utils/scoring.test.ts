import {describe,expect,it} from "vitest";import {movement,weightedScore} from "./scoring";import {DIMENSION_WORKFLOWS} from "../types";describe("scoring",()=>{it("calculates weighted score",()=>expect(weightedScore(DIMENSION_WORKFLOWS.map(d=>({dimensionId:d.id,score:4,evidence:""})),DIMENSION_WORKFLOWS)).toBe(4));it("calculates movement",()=>expect(movement(4.2,3.7)).toBe(0.5));});