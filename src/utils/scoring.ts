import type {AssessmentScore,Dimension} from "../types"; export function weightedScore(scores:AssessmentScore[],dimensions:Dimension[]){const map=new Map(dimensions.map(d=>[d.id,d]));const weight=scores.reduce((n,s)=>n+(map.get(s.dimensionId)?.weight??0),0);return weight?scores.reduce((n,s)=>n+s.score*(map.get(s.dimensionId)?.weight??0),0)/weight:0} export function movement(current:number,previous:number){return Number((current-previous).toFixed(2))}