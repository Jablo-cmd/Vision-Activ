import type {AssessmentScore,DimensionWorkflow} from "../types";export function weightedScore(scores:AssessmentScore[],dimensions:DimensionWorkflow[]){const map=new Map(dimensions.map(d=>[d.id,d]));const weight=scores.reduce((n,s)=>n+(map.has(s.dimensionId)?1:0),0);return weight?scores.reduce((n,s)=>n+s.score,0)/weight:0}export function movement(current:number,previous:number){return Number((current-previous).toFixed(2))}