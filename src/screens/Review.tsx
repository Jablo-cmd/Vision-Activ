import {useState} from "react"; import {Button,Card} from "../components/ui"; import {saveReview} from "../services/data";
export function Review(){const[notes,setNotes]=useState(""),[barriers,setBarriers]=useState(""),[support,setSupport]=useState(""),[saved,setSaved]=useState(false);const save=async()=>{await saveReview({id:crypto.randomUUID(),assessmentId:"current-week",notes,barriers,support,reviewedAt:new Date().toISOString()},"current-user");setSaved(true)};return <div className="space-y-6"><div><h1 className="text-3xl font-bold text-[#071a35]">Management Review</h1><p className="mt-2 text-slate-500">A focused 20-minute CEO or manager check-in centred on evidence, barriers and support.</p></div><Card className="p-6"><div className="grid gap-5"><label className="text-sm font-semibold">What changed since the last review?<textarea value={notes} onChange={e=>setNotes(e.target.value)} className="mt-2 min-h-28 w-full rounded-xl border border-slate-200 p-3"/></label><label className="text-sm font-semibold">What barriers are affecting performance?<textarea value={barriers} onChange={e=>setBarriers(e.target.value)} className="mt-2 min-h-28 w-full rounded-xl border border-slate-200 p-3"/></label><label className="text-sm font-semibold">What support or decisions are required?<textarea value={support} onChange={e=>setSupport(e.target.value)} className="mt-2 min-h-28 w-full rounded-xl border border-slate-200 p-3"/></label></div><div className="mt-5 flex items-center justify-end gap-3"><span className="text-sm text-slate-500">{saved?"Review saved.":""}</span><Button onClick={save} className="bg-orange-500 text-white">Save review</Button></div></Card></div>}