import {useState} from "react"; import {Button,Card} from "../components/ui"; import {saveReview,getMyOrganization} from "../services/data";import {supabase} from "../services/supabase";
export function Review(){const[notes,setNotes]=useState(""),[barriers,setBarriers]=useState(""),[support,setSupport]=useState(""),[saved,setSaved]=useState(false);const save=async()=>{const org=await getMyOrganization();if(!org?.organization_id)throw new Error("Account is not assigned to an organisation.");const user=await supabase!.auth.getUser();await saveReview({id:crypto.randomUUID(),assessmentId:"",notes,barriers,support,reviewedAt:new Date().toISOString()},org.organization_id,user.data.user?.id||"");setSaved(true)};return <div className="space-y-6"><div><h1 className="text-3xl font-bold text-[#172B4D]">Management Review</h1><p className="mt-2 text-[#667085]">A focused 20-minute CEO or manager check-in centred on evidence, barriers and support.</p></div><Card className="p-6"><div className="grid gap-5"><label className="text-sm font-semibold text-[#263238]">What changed since the last review?<textarea value={notes} onChange={e=>setNotes(e.target.value)} className="mt-2 min-h-28 w-full rounded-xl border border-slate-200 p-3 focus:border-[#0072CE] focus:outline-none"/></label><label className="text-sm font-semibold text-[#263238]">What barriers are affecting performance?<textarea value={barriers} onChange={e=>setBarriers(e.target.value)} className="mt-2 min-h-28 w-full rounded-xl border border-slate-200 p-3 focus:border-[#0072CE] focus:outline-none"/></label><label className="text-sm font-semibold text-[#263238]">What support or decisions are required?<textarea value={support} onChange={e=>setSupport(e.target.value)} className="mt-2 min-h-28 w-full rounded-xl border border-slate-200 p-3 focus:border-[#0072CE] focus:outline-none"/></label></div><div className="mt-5 flex items-center justify-end gap-3"><span className="text-sm text-[#667085]">{saved?"Review saved.":""}</span><Button onClick={save} className="bg-[#F58220] text-white hover:bg-[#D96D12]">Save review</Button></div></Card></div>}
