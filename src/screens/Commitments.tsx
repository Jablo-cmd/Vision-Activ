import {useState} from "react";import {Plus} from "lucide-react";import {Button,Card} from "../components/ui";import {DIMENSION_WORKFLOWS,type Commitment} from "../types";import {saveCommitment,getMyOrganization} from "../services/data";
export function Commitments(){const[items,setItems]=useState<Commitment[]>([]);const[form,setForm]=useState({dimensionId:DIMENSION_WORKFLOWS[0].id,title:"",action:"",timeframe:"",evidence:""});const add=async()=>{if(!form.title||!form.action)return;const item={...form,id:crypto.randomUUID(),status:"not_started" as const};setItems(p=>[item,...p]);const org=await getMyOrganization();if(!org?.organization_id)throw new Error("Account is not assigned to an organisation.");await saveCommitment(item,org.organization_id);setForm(p=>({...p,title:"",action:"",timeframe:"",evidence:""}))};return <div className="space-y-6"><div><h1 className="text-3xl font-bold text-[#071a35]">Personal Improvement Commitment Charter</h1><p className="mt-2 text-slate-500">Create a specific commitment against a framework dimension, with timeframe and observable evidence.</p></div><Card className="p-6"><h2 className="font-bold">New PICC commitment</h2><div className="mt-4 grid gap-3 md:grid-cols-2"><select value={form.dimensionId} onChange={e=>setForm(p=>({...p,dimensionId:e.target.value}))} className="rounded-xl border border-slate-200 px-4 py-3">{DIMENSION_WORKFLOWS.map(d=><option key={d.id} value={d.id}>{d.name}</option>)}</select><input value={form.title} onChange={e=>setForm(p=>({...p,title:e.target.value}))} placeholder="Commitment title" className="rounded-xl border border-slate-200 px-4 py-3"/><input value={form.action} onChange={e=>setForm(p=>({...p,action:e.target.value}))} placeholder="Specific action" className="rounded-xl border border-slate-200 px-4 py-3"/><input value={form.timeframe} onChange={e=>setForm(p=>({...p,timeframe:e.target.value}))} placeholder="Timeframe" className="rounded-xl border border-slate-200 px-4 py-3"/><input value={form.evidence} onChange={e=>setForm(p=>({...p,evidence:e.target.value}))} placeholder="Evidence of improvement" className="rounded-xl border border-slate-200 px-4 py-3 md:col-span-2"/></div><Button onClick={add} className="mt-4 bg-[#071a35] text-white"><Plus size={16} className="mr-2 inline"/>Add commitment</Button></Card><div className="grid gap-4">{items.map(item=><Card key={item.id} className="p-5"><div className="flex items-start justify-between gap-4"><div><div className="text-xs font-bold uppercase tracking-wider text-orange-600">{DIMENSION_WORKFLOWS.find(d=>d.id===item.dimensionId)?.name}</div><h3 className="mt-1 font-bold text-[#071a35]">{item.title}</h3><p className="mt-1 text-sm text-slate-600">{item.action}</p></div><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold text-slate-600">{item.status.replace("_"," ")}</span></div><div className="mt-4 grid gap-2 text-xs text-slate-500 md:grid-cols-2"><div>Timeframe: <strong className="text-slate-700">{item.timeframe||"Not specified"}</strong></div><div>Evidence: <strong className="text-slate-700">{item.evidence||"Not specified"}</strong></div></div></Card>)}</div></div>}