import {useState} from "react"; import {Plus} from "lucide-react"; import {Button,Card} from "../components/ui"; import {saveCommitment} from "../services/data"; import type {Commitment} from "../types"; export function Commitments(){const[items,setItems]=useState<Commitment[]>([{id:"1",title:"Weekly planning discipline",action:"Complete weekly priorities before Monday 09:00.",timeframe:"Weekly",evidence:"Planning record",status:"in_progress"},{id:"2",title:"Close recurring blocker",action:"Resolve the identified process bottleneck and document the outcome.",timeframe:"30 days",evidence:"Outcome record",status:"in_progress"}]);const[form,setForm]=useState({title:"",action:"",timeframe:"",evidence:""});const add=async()=>{if(!form.title||!form.action)return;const item={...form,id:crypto.randomUUID(),status:"not_started" as const};setItems(p=>[item,...p]);await saveCommitment(item,"current-user");setForm({title:"",action:"",timeframe:"",evidence:""})};return <div className="space-y-6"><div><h1 className="text-3xl font-bold text-[#071a35]">Personal Improvement Commitment Charter</h1><p className="mt-2 text-slate-500">Turn assessment findings into specific, observable commitments.</p></div><Card className="p-6"><h2 className="font-bold">New commitment</h2><div className="mt-4 grid gap-3 md:grid-cols-2">{Object.entries(form).map(([key,value])=><input key={key} value={value} onChange={e=>setForm(p=>({...p,[key]:e.target.value}))} placeholder={key==="title"?"Commitment title":key==="action"?"Action to take":key==="timeframe"?"Timeframe":"Evidence of improvement"} className="rounded-xl border border-slate-200 px-4 py-3 outline-none focus:border-orange-500"/>)}</div><Button onClick={add} className="mt-4 bg-[#071a35] text-white"><Plus size={16} className="mr-2 inline"/>Add commitment</Button></Card><div className="grid gap-4">{items.map(item=><Card key={item.id} className="p-5"><div className="flex items-start justify-between gap-4"><div><h3 className="font-bold text-[#071a35]">{item.title}</h3><p className="mt-1 text-sm text-slate-600">{item.action}</p></div><span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-700">{item.status.replace("_"," ")}</span></div><div className="mt-4 grid gap-2 text-xs text-slate-500 md:grid-cols-2"><div>Timeframe: <strong className="text-slate-700">{item.timeframe}</strong></div><div>Evidence: <strong className="text-slate-700">{item.evidence}</strong></div></div></Card>)}</div></div>}